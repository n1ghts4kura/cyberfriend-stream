# PROJECT_TARGET_COPILOT.backend

更新时间: 2025-08-31
状态: 初始草案 (仅包含已确认高层共识 + 占位，不展开实现细节)

## 1. 总体使命 (已确认)
后端负责生成抽象事件驱动前端 Live2D 表现，前端无需理解 Cubism 细节。并包含音频管线占位：系统扬声器音频捕获 → 语音转文字(ASR，具体组件待定) → 输入 AI Agent → 获得回复文本 → cozyvoice2 文字转语音 → 生成含文本与音频引用的事件推送前端。

## 2. 通信方式 (已确认)
单向下行 WebSocket 事件流 (JSON 文本帧)。客户端当前不发送 ack。所有事件立即包含唯一 id，为未来 ack / 重发 / 幂等预留。

## 3. 事件统一外壳 (已确认)
采用“统一事件”抽象：每个事件具备统一外层元数据(含唯一 id 与 type 等)，但当前阶段不确定具体字段名称/完整集合，仅锁定存在统一包装与唯一 id 的原则；具体 payload 结构延后。

## 4. 前后端职责边界 (已确认)
当前阶段：后端只决定何时发出哪些事件（可附带占位 priority 字段）；前端采取最简单策略——后到事件直接触发或覆盖播放。不实现复杂优先级评估 / 打断 / 状态机。高级调度标记为后续里程碑。

## 5. 后端模块范围 (已确认)
本阶段仅实现：
- Transport: FastAPI WebSocket /health。
- Protocol: 事件封装/序列化(统一外壳 + id)。
- Agent 管线: 文本进 / 文本出 (动作意图后续细化)。
- 音频管线占位: 捕获 → ASR → Agent → TTS → 事件。
- Logging: 基础日志。
延期/未创建：资源映射表、事件调度器、重发机制、鉴权、复杂意图映射、闲置/Idle 逻辑等。

## 6. FastAPI 端点 (已确认)
- /ws : WebSocket 下行事件通道。
- /health : 基础健康检查。
暂不提供 /chat 之类手动触发接口；若调试需要可后续添加。

## 7. 事件来源 (占位)
不细化分类；仅记录会在“启动、语音处理完成、适当场合”产生事件。Idle / 手动触发 / 其它类别暂不纳入。

## 8. 优先级 (占位)
存在 future 概念 (force / normal / idle 等)；当前不使用与不解释语义，纯占位。

## 9. 资源 / 模型配置 (跳过)
本阶段不设计统一资源映射或模型目录协议；需要引用的资源（如音频文件）可直接用文件路径或 URL 放入事件。未来若需再集中化。

## 10. 语音管线 (高层占位)
仅确立宏观流程；ASR 组件选择、分段策略、缓存命名、音频事件字段细节后续再讨论。

## 11. AI Agent (占位)
当前只需“输入文本 → 输出回复文本”；动作/表情意图、工具调用策略与上下文记忆机制延后细化。

## 12. 错误处理 (占位)
仅写入后端日志。不定义统一错误分类或前端 error 事件广播；是否推送 error 事件后续决定。

## 13. 配置与环境变量 (已确认)
使用 python-dotenv 加载 .env。最小键：OPENAI_API_KEY，ASR_ENABLED (可选)，TTS_ENABLED (可选)。其他如端口、日志级别、模型路径使用代码默认，可被同名环境变量覆盖。不实现复杂配置系统。

## 14. 日志策略 (已确认)
使用标准 logging 输出到控制台；不做结构化 JSON 或多 channel 分离。需要时再扩展。

## 15. 性能与并发 (占位)
假设单实例低并发本地运行；不做专项优化。若 TTS / ASR 阻塞造成卡顿，再讨论线程/进程/队列。

## 16. 安全与鉴权 (占位)
当前无鉴权、无速率限制、无事件白名单强化逻辑。未来可能引入 token、速率限制、来源校验。

## 17. 部署与运行 (占位)
仅支持本地 uvicorn + .env 运行；不提供容器化、多实例、热升级设计。需求出现再扩展。

## 18. 测试策略 (占位)
当前不编写自动化测试；未来若增强可靠性再添加单测/集成测试（事件封装、WebSocket 连通等）。

## 19. 文档与版本 (已确认)
本文件为后端高层共识基线。细节（协议字段、ASR 选择、Agent 意图映射、事件分类、资源策略等）后续通过“Revision”段落迭代增补。

## 20. 未来潜在扩展列表 (记录)
- Ack & 重发 / 幂等处理。
- 优先级策略与动作打断。
- 资源映射与缓存策略。
- Idle / 定时事件生成。
- 鉴权、速率限制、安全白名单。
- 可选 REST 控制接口 (/chat, /action)。
- 结构化日志与事件追踪。
- 自动化测试与性能基准。
- 多模型 / 多实例支持。

## 21. 已确认 vs 占位对照
| 编号 | 条目 | 状态 |
| ---- | ---- | ---- |
| 1 | 总体使命 + 音频管线宏观 | 已确认 |
| 2 | 通信方式 + id 预留 | 已确认 |
| 3 | 统一事件外壳抽象 | 已确认 |
| 4 | 职责边界 & 不做调度 | 已确认 |
| 5 | 模块范围裁剪 | 已确认 |
| 6 | FastAPI 端点最小集 | 已确认 |
| 7 | 事件来源笼统占位 | 占位 |
| 8 | 优先级占位 | 占位 |
| 9 | 资源配置跳过 | 占位 |
| 10 | 语音管线宏观 | 占位 |
| 11 | AI Agent 高层 | 占位 |
| 12 | 错误处理日志化 | 占位 |
| 13 | 配置与 dotenv | 已确认 |
| 14 | 日志策略基础 | 已确认 |
| 15 | 性能假设 | 占位 |
| 16 | 安全鉴权 | 占位 |
| 17 | 部署运行 | 占位 |
| 18 | 测试策略 | 占位 |
| 19 | 文档与版本 | 已确认 |
| 20 | 扩展列表 | 记录 |

## 22. Revision
- 2025-08-31: 初始草案创建。

---
后续更新：在 Revision 中追加日期与摘要，并仅修改对应小节内容。
